{
  "name": "trackable",
  "version": "0.0.1",
  "types": "dist/index.d.ts",
  "main": "dist/index.js",
  "files": [
    "dist/**/*"
  ],
  "description": "",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.5.4",
    "tslib": "^2.3.1",
    "vitest": "^0.34.2"
  }
}
